package com.gamater.sdk.imagepicker.util;

public class GlideHelper {
	/*
	 * private static RequestManager requestManager;
	 * 
	 * public static RequestManager getRequestManager(Context context) { if
	 * (requestManager == null) { requestManager =
	 * Glide.with(context.getApplicationContext()); } return requestManager; }
	 */
}
